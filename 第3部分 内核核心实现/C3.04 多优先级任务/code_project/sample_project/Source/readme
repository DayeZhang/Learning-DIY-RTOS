1、添加配置文件，写入优先级数量；
2、修改任务表，使用优先级, 去掉固定数量；
3、修改任务结构，添加优先级字段, 在初始化函数中添加优先级；
4、修改main, 去掉填优先级表，去掉idleTask变量；
5、去掉指定某个任务直接运行，而是通过先运行
6、实现tTaskHighestReady；
7、修改调底算法，优选最高优先级的任务去运行；
8、修改task1和task2，什么都不做；
9、测试，总是运行任务1，不会跑task2，即便是在中断中进行调度，也不会切换

引子：如何任务延时了应该怎么办？